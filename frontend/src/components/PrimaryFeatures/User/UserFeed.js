const UserFeed = ({ dataObject, usersSize, usersAlias }) => {

    return (
        <div className="user-feed glass">
            <h5>Temp User Feed</h5>
        </div>
    )
}

export default UserFeed
