import { useState } from "react";

const EditTags = ({ tags, tagsSize, tagsAlias }) => {

    const currentTags = tags
    const [editMode, setEditMode] = useState();



    return (
        <div className='edit-tags'>
            TestyPoo
        </div>
    )
}

export default EditTags
